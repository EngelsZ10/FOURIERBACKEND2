{
    "builds": [{
        "src": "FOURIERAPI/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [{
        "src": "/(.*)",
        "dest": "FOURIERAPI/wsgi.py"
    }]
}